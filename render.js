window.$ = window.jQuery = require('jquery')

const fs = require('fs')
const process = require('process')

let currentDir = '/home/kibin/'

// fs.readdir(currentDir, (err, files) => {
//     console.log(files)
// })

